# Production Environment Configuration
# Copy this file and set these variables in your Render.com dashboard

NODE_ENV=production
PORT=5000

# Production MySQL Database (use your production database URL)
DB_HOST=your-production-db-host.com
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_secure_password
DB_NAME=library

# Session Secret (REQUIRED - use a strong random string)
SESSION_SECRET=your-strong-random-secret-here-min-32-chars

# Redis URL (HIGHLY RECOMMENDED for production to persist sessions)
# Without Redis, sessions will be lost on server restart
# Get free Redis at: https://upstash.com/ or https://redis.com/
REDIS_URL=redis://your-redis-url-here

# Frontend URLs (add all your Vercel deployment URLs)
FRONTEND_URL=https://library-mu-sable.vercel.app

# Backend URL (your Render deployment URL)
BACKEND_URL=https://library-backend-2-uvqp.onrender.com

# Cloudinary (for profile image uploads in production)
CLOUDINARY_CLOUD_NAME=your_cloudinary_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret
