import React from 'react';

const Navbar = () => {
  return (
    <nav className="navbar">
      <button>Currently Borrowed Books</button>
      <button>Borrow History</button>
    </nav>
  );
};

export default Navbar;
